[package]
name = "darkirc-channel"
version = "1.1.6"
edition = "2021"
description = "DarkIRC WASM channel for LunarWing"
license = "AGPL-3.0-or-later"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Must match ironclaw's Telegram channel (channels-src/telegram/Cargo.toml)
wit-bindgen = "0.36"

[profile.release]
opt-level = "s"
lto = true
strip = true

[workspace]
