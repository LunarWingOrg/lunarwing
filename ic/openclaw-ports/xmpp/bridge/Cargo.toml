[package]
name = "openclaw-xmpp-bridge-contract"
version = "0.1.0"
edition = "2024"
description = "Shared request/response contract for the local LunarWing XMPP bridge"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }

[lib]
path = "src/lib.rs"

[workspace]
